import { ReadExpenseFromExcel } from './read-expense-from-excel';

describe('ReadExpenseFromExcel', () => {
  it('should create an instance', () => {
    expect(new ReadExpenseFromExcel()).toBeTruthy();
  });
});
