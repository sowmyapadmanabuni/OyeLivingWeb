import { UnitListForRoleChange } from './unit-list-for-role-change';

describe('UnitListForRoleChange', () => {
  it('should create an instance', () => {
    expect(new UnitListForRoleChange()).toBeTruthy();
  });
});
